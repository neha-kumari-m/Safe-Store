Sampling algorithm is a powerful technique in data mining that selects a representative portion of data from dataset for analysis, while maintaining the important characteristics of the original data. Here we select a small subset of data (sample) from the large dataset where sample represents the whole data. Instead of analyzing whole data which can be very long and time consuming, we analyze the sample to save time, memory and required resources.